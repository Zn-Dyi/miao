doctype html

html
  head
    meta(charset="UTF-8")
    title 我的论坛
    block scripts
      script(src='/jquery.min.js')
  body
    header
      a(href="/") 首页
      |
      |
      div.header
        if loginName
          span(title=`hi, ${loginName}`) welcome, #{loginName}
          |
          |
          a(href="/add-post") 发贴
          |
          |
          a(href="/logout") 登出
        else
          a(href="/register.html") 注册
          |
          |
          a(href="/login") 登入
    block content
    block foot
      #footer
        p &copy;
    //- script.
    //-   var cookies = document.cookie
    //-   var cookiesObj = cookies.split('; ').reduce((obj, cookie) => {
    //-     var [key, val] = cookie.split('=')
    //-     obj[key] = val
    //-     return obj
    //-   }, {})

    //-   if (cookiesObj.loginName) {
    //-     document.querySelector('div.header').innerHTML = '欢迎'
    //-   } else {
    //-     document.querySelector('div.header').innerHTML = '<a href="/login">登陆</a>'
    //-   }
